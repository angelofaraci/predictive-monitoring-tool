"""Server-rendered Jinja2 + HTMX presentation layer (Phase 8).

Thin routing/view package on top of the existing `data`/`api` modules —
no business logic is duplicated here (spec: dashboard-ui Phase 8).
"""
