.atl/
.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage

# Terraform (infra/terraform) — local state and working-dir cache only.
# .terraform.lock.hcl stays tracked (provider version pinning).
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
crash.log

# Trained model artifacts (Phase 3) — regenerable via `models/train.py`; not committed.
models/*.joblib
models/*.metadata.json

# Per-installation real-trained model artifacts (models/resolver.py's
# REAL_MODEL_DIR default) — installation-specific, never committed.
models/real/

# Prometheus connection config (Phase 1.6) — holds a user-specific URL; not committed.
config/prometheus.json

docs/local/

# Design handoff assets (not part of the tracked codebase).
Sentinel AIOps_ Interfaz de monitoreo/
